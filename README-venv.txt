2)  virtual enviroment
    desde la consola, adentro de la carpeta del proyecto

    >python -m venv venv

    El proyecto necesita flask, as[i que uso pip para instalarlo
    desde la carpeta scripts>pip install flask
